This file should go in /etc/init.d to allow servers to be automatically started/stopped
when the computer changes power states.  An example server is set up.  The su command
is important because without it all the servers would be run as root.